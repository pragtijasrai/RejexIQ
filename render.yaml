services:
  - type: web
    name: rejexiq-app
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.x
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
